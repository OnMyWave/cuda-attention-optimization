name: cuda-opt
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch>=2.0.0
  - pytorch-cuda=12.1
  - cudatoolkit
  - numpy
  - matplotlib
  - pytest
  - pip
  - pip:
    - torch>=2.0.0
